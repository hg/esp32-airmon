#pragma once

#include "co2.h"
#include "dallas.h"
#include "pms.h"
#include <driver/gpio.h>

static co2_sensor co2_sensors[] = {};

static temp_sensor temp_sensors[] = {
    {
        .name = "room",
        .pin = GPIO_NUM_5,
        .queue = NULL,
    },
};

static pm_sensor pm_sensors[] = {
    {
        .name = "room",
        .port = UART_NUM_1,
        .rx = GPIO_NUM_25,
        .tx = GPIO_NUM_27,
        .queue = NULL,
    },
};
