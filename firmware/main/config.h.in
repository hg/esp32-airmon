#pragma once

#include "co2.h"
#include "dallas.h"
#include "pms.h"
#include <driver/gpio.h>

static co2::Sensor co2Sensors[] = {};

static ds::TempSensor tempSensors[] = {
    {
        .name = "room",
        .pin = GPIO_NUM_5,
        .queue = nullptr,
    },
};

static pms::Station pmsStations[] = {
    {
        .name = "room",
        .port = UART_NUM_1,
        .rxPin = GPIO_NUM_25,
        .txPin = GPIO_NUM_27,
        .queue = nullptr,
    },
};